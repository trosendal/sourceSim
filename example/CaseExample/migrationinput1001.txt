0		0		0.005		
0.005	0		0.01 	
0		0.01 	0		


#This is input parameter file for giving specific pop_i->pop_j migration rates. Row index = Source population index, Column index = Destination population index.
#Use tab or space to separate values.
#Too few values will cause program to exit upon reading. Excess values will only be ignored.
#All values where i=j will be set to zero. If such parameter was given as non-zero, the element will be marked with "!0".

# Free comment space below until three stars (*). These and the parameters are included in the rundetails***.txt and can be used for description of the run:

Low lambda run settings for case example.

***
